<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <%= @users.each do |user| %>

      <p><%= link_to "#{user.email}", user %></p>

      <% end %>
    </div>
  </div>
</div>
