module WikisHelper

  # def verified_to_view(user, wiki)
  #
  #   if wiki.private?
  #     if wiki.user || current_user.admin? ||
  #
  #
  #    && wiki.user != current_user && !current_user.admin? || wiki.collaborators.include?(current_user)
  #
  # end

end
